# Codex Rules

- Add artworks through the data system only. Create or update Markdown records in `src/data/artworks/` using `npm run new-artwork` or the same frontmatter schema; do not hardcode artworks directly into `/artist/everything` or archive components.
- Keep unknown artwork metadata blank. Do not invent dates, media, dimensions, locations, descriptions, or publication status.
